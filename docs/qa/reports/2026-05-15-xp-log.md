# SchreckNet QA Report — XP Log + Undo (PR #281 / main HEAD `54343a3`)

**Date:** 2026-05-15
**Branch under test:** `qa-xp-log` (off `origin/main` @ `54343a3` — "XP log + undo for VtM / Werewolf / Hunter (#281)")
**Tester:** Claude (automated, Playwright MCP)
**Account:** `testxp@test.com` / `TestPass123!` (user_id 42, pre-existing)

---

## 1. Summary

| Metric | Count |
|---|---|
| Game lines verified (XP Log UI present) | 3 / 3 — VtM, Werewolf, Hunter |
| Full character-creation flows driven end-to-end | 1 (VtM — saved as id 103) |
| XP Log + Undo flows exercised end-to-end | VtM (full); Werewolf, Hunter (UI render smoke only) |
| Stress tests executed | 10× alternating buy/undo (passed); 10× sequential buy then 10× sequential undo (passed) |
| Persistence round-trips | 1 (Update Character → page reload → state preserved ✓) |
| Defects found | 1 High, 1 Low, plus 1 pre-existing Low (noted) |

**Headline result:** the **basic XP Log + Undo flow works** for VtM single-purchase upgrades (attribute/skill levels, advantages/flaws via the summary +3xp button, etc.). The Undo confirmation dialog, top-only Undo button, log-entry rendering, and persistence across reload all behave as specified.

**The PR's own "known concern" about within-session `priorValue` is real and reproducible.** It manifests in the `advantage` (and by code-review, `flaw`) handlers when multiple delta-style purchases are made in a single Spend XP dialog session. Net end-state after fully undoing the session is correct, but intermediate state after a partial undo is wrong — see Defect 1.

---

## 2. Defects

### Defect 1 — Multi-buy advantage/flaw undo overshoots intermediate state

- **Severity:** High (correctness of advertised feature on a release branch)
- **Title:** Undoing an advantage purchase made in a multi-buy Spend XP session restores `advantages_remaining` to the *session-start* value instead of the *immediately-prior* value
- **Game lines affected:** VtM (empirically confirmed), Werewolf (source review — same pattern), Hunter (source review — same pattern, slightly different state shape so needs empirical verification)
- **Known to PR author:** yes — flagged in the testing brief as "PR #281: within-session priorValue may be wrong"

#### Steps to reproduce (VtM)

1. Log in. Open an editable VtM character. Make sure baseline Advantage Dots Remaining is known (e.g. `7`) and Remaining XP is enough (e.g. `50`).
2. Open **Spend XP**. Pick category **Advantage**. Select dots = `2`. Click **Purchase**. Don't close yet.
3. In the same dialog, select **Advantage** again, dots = `3`, click **Purchase**.
4. Click **Save** on the Spend XP dialog.
5. Open **XP Log**. Two entries appear, newest first: `Added 3 advantage point(s)` (top) and `Added 2 advantage point(s)` (bottom).
6. Click **Undo** on the top entry, confirm.

#### Expected

- Remaining XP: `35 + 9 = 44` ✓
- Advantage Dots Remaining: `12 − 3 = 9`
- The bottom entry remains, still showing `Added 2 advantage point(s)`.

#### Actual

- Remaining XP: `44` ✓
- **Advantage Dots Remaining: `7`** ✗ (jumped past `9` straight to the session-start baseline)
- Bottom entry remains visible. If the user then undoes it: XP `+6 → 50` (correct), Advantage Dots `→ 7` (no change — already there). The *net* end-state of fully undoing the session is correct (only because both entries restore to the same baseline), but a user partially undoing will see a wrong dot count and wrong XP-vs-dots accounting.

#### Root cause (source review)

- [src/components/character_creator/vtm/spendXp.vue:467-471](src/components/character_creator/vtm/spendXp.vue#L467-L471) — when "Purchase" is clicked for an Advantage, `handlers.advantage.record(this, ...)` is called with `this` as state. The handler reads `this.advantages_remaining`, which was seeded once at dialog open from `props.info.advantages_remaining` (line 279) and **is not mutated during the dialog**. The local accumulator `this.advantagePoints` is the delta and is incremented separately on line 471. So every `record(...)` call within the same dialog sees the same starting `advantages_remaining`, and every recorded entry carries the same `priorValue`.
- [src/lib/xp/handlers/vtm.js:228](src/lib/xp/handlers/vtm.js#L228) — `undo` is `state[entry.payload.counter] = entry.payload.priorValue;`, i.e. it *assigns* the snapshot back rather than reversing the recorded `delta`. When two entries share the same `priorValue`, undoing the top entry overshoots.
- Same pattern in [src/components/character_creator/werewolf/spendXp.vue:539-543](src/components/character_creator/werewolf/spendXp.vue#L539-L543) and the matching `undo` in [src/lib/xp/handlers/werewolf.js:230-232](src/lib/xp/handlers/werewolf.js#L230-L232).
- Same pattern in [src/components/character_creator/hunter/spendXp.vue:346-350](src/components/character_creator/hunter/spendXp.vue#L346-L350) and [src/lib/xp/handlers/hunter.js:153](src/lib/xp/handlers/hunter.js#L153). (Hunter builds a fresh `handlerState` object each call from `this.advantagePoints`, an accumulator that starts at `0` — so behavior in Hunter is slightly different and worth empirically rechecking.)

#### Why the unit tests don't catch it

[src/lib/xp/handlers/\_\_tests\_\_/vtm.test.js:285-301](src/lib/xp/handlers/__tests__/vtm.test.js#L285-L301) and the `flaw` test directly below it only test the *single*-record → single-undo cycle. They manually set `charState.advantages_remaining = 6` between `record` and `undo` to simulate the parent applying the delta — but that simulation never produces two records with the same `priorValue` against a state that has been incremented twice. Suggested test addition:

```js
const charState = { advantages_remaining: 5 };
const e1 = handlers.advantage.record(charState, { delta: 1 });
charState.advantages_remaining = 6;
const e2 = handlers.advantage.record(charState, { delta: 1 }); // priorValue should be 6, will probably be 5
expect(e2.payload.priorValue).toBe(6);
```

…and a parallel integration test where the *dialog component* records two entries without the parent mutating state in between.

#### Suggested fix

Two options:

1. Update `advantage` / `flaw` `undo` to reverse by `delta`: `state[counter] -= entry.payload.delta;` instead of `= priorValue`. This is robust regardless of how `record` captured `priorValue`.
2. Or, in the Spend XP dialog, mutate `this.advantages_remaining` (in addition to `this.advantagePoints`) after each record so the next `record(...)` sees the updated state.

Option 1 is the smaller blast radius. Option 2 also makes the dialog's running display match the post-save reality, which is friendlier.

#### Evidence

- Screenshot of the broken intermediate state: [2026-05-15-xp-log-screenshots/vtm-adv-dot-bug-after-first-undo.png](2026-05-15-xp-log-screenshots/vtm-adv-dot-bug-after-first-undo.png) — shows Advantage Dots Remaining = 7 with one log entry (`Added 2 advantage point(s)`) still present and the wrong XP/dots accounting visible on the same screen.

---

### Defect 2 — "Cost to Purchase: NaN" shows before a sub-selection is made

- **Severity:** Low (cosmetic; users complete the flow)
- **Title:** In the Spend XP dialog, after picking a category (e.g. "Attributes") but before picking the sub-target (e.g. which attribute), the dialog renders `Cost to Purchase: NaN`.
- **Steps:** Open Spend XP → choose "Attributes" → before choosing an attribute, observe the displayed text.
- **Expected:** Either hide the cost row until a sub-selection is made, or show `Cost to Purchase: —`.
- **Actual:** `Cost to Purchase: NaN`.
- **File hypothesis:** [src/components/character_creator/vtm/spendXp.vue](src/components/character_creator/vtm/spendXp.vue) — the computed `cost` reactively binds before all of its operands are defined. Guard with a `v-if` or a fallback in the template.

---

### Defect 3 — `adsbygoogle.push()` error fires on every client-side route change (pre-existing, noted)

- **Severity:** Low / pre-existing
- **Title:** Every `$route` watcher tick in `MainLayout.vue:116` calls `adsbygoogle.push({})`, which throws `TagError: All 'ins' elements in the DOM with class=adsbygoogle already have ads in them.` on subsequent SPA navigations.
- **Why I'm including this:** it pollutes the browser console after any save/navigate, and during the QA pass it kept counting against the "Console errors" tally in Playwright output. Not related to XP Log, but worth tidying up — guard the `push` so it only fires for ads not yet initialized, or move it out of the router watcher.
- **File:line:** [src/layouts/MainLayout.vue:116](src/layouts/MainLayout.vue#L116) (per the stack trace in the console message).

---

## 3. What was verified (in detail)

### 3.1 VtM character creation, end-to-end (Brujah / Alleycat)

- Created `QA Tester Vamp` (id 103). Filled Core Concept (name, concept, ambition, desire, archetype, chronicle, sire). Attributes 4/3/3/3/2/2/2/2/1 = 22 points ✓. Skills via Jack-of-all-trades distribution (1×3 + 8×2 + 10×1 = 29 points) ✓. One specialty (Brawl: Kindred). Conviction + matching Touchstone. Clan dialog: Brujah, disciplines Celerity 2 / Potence 1 ✓. Predator type Alleycat (gain Celerity dot, lose 1 Humanity, Intimidation specialty). Discipline-skill picks. Save succeeded, URL redirected to `/vampire/5e/103`. Humanity correctly reduced from `7` to `6` by Alleycat. Health derivations (Stamina + 3 = 6) and Willpower (Composure + Resolve = 4) all consistent.
- Reload of `/vampire/5e/edit/103` shows the saved state intact.

### 3.2 XP Log + Undo basics (VtM)

- **Set XP input** correctly **does NOT** create a log entry (matches spec: "manual `Set XP` input does NOT create a log entry").
- Buying Intelligence `1 → 2` via Spend XP creates a log entry: `Raised Intelligence to 2 · Cost: 10 XP · Balance after: 40`. ✓
- Undo confirmation dialog appears and reads `Undo this purchase? Raised Intelligence to 2 — you'll get 10 XP back.` Cancel / Undo buttons present.
- Confirming Undo: XP restored `40 → 50`, attribute restored `Intelligence 2 → 1`, log emptied. ✓
- Screenshot: `vtm-xp-log-after-purchase.png`, `vtm-undo-confirm.png`.

### 3.3 Attribute multi-buy in one Spend XP session — undo correctness (PASSED)

- Bought Intelligence `1→2` (10xp) then `2→3` (15xp) in the same Spend XP dialog. Saved.
- Log shows two entries. Only the top entry has an Undo button (matches spec).
- Undo top: Intelligence `3 → 2`, XP `25 → 40`. ✓
- Undo bottom (now top): Intelligence `2 → 1`, XP `40 → 50`. ✓
- **Attribute upgrades are immune to the multi-buy bug** because each level upgrade is its own record with its own `priorValue` (the per-level value, not a session-start snapshot).

### 3.4 Advantage multi-buy in one Spend XP session — undo correctness (FAILED — Defect 1)

- Documented in Defect 1.

### 3.5 ±3xp summary buttons

- Clicking `+` on `Advantage Dots Remaining` in the character summary: dots `7→8`, XP `50→47`. Creates a log entry: `Added 1 advantage point(s) · Cost: 3 XP`. Undoing that single entry correctly restores `8→7` and `47→50`. ✓
- Each click of the `+` button creates a *separate* log entry, so each entry's `priorValue` is correct for its own moment. The bug from Defect 1 does **not** manifest when using the summary `+3xp` button repeatedly (as opposed to multi-buy inside one Spend XP dialog).

### 3.6 Stress test — 10× sequential buy then 10× sequential undo

- 10 consecutive `+3xp` clicks on Advantage. State linearly progressed `XP 47 → 17` (Δ −30 ✓) and `AdvDots 8 → 18` (Δ +10 ✓).
- 10 consecutive Undo confirmations. State linearly walked back `XP 17 → 47` (each undo +3) and `AdvDots 18 → 8` (each undo −1). Every intermediate state correct.

### 3.7 Stress test — 10× tight alternation (buy → open log → undo → close)

- Each cycle returned the page to baseline `XP=47, AdvDots=8`. State remained consistent for all 10 cycles.

### 3.8 Persistence

- `Update Character` (server save) → full page reload → all state (XP, advantage dots, attribute levels) preserved.

### 3.9 Werewolf + Hunter — UI smoke

- Navigated to `/werewolf/5e/edit/5` (Bjorn Harding) and `/hunter/5e/edit/48` (fwefwe).
- Both pages render the same edit-page furniture: Attributes/Skills/etc nav, **XP Log** nav item, **Spend XP** nav item, summary panel with ±3xp `Advantage Dots Remaining` / `Flaw Dots Remaining` buttons.
- Clicking XP Log on each opens the dialog and reads the character's XP balance and "No XP transactions logged yet" empty state.
- I did not drive Spend XP / Undo flows on W or H because (a) the existing characters belong to other users, so save would likely 403, and (b) creating brand-new W/H characters end-to-end through the UI would have blown the remaining time budget. The source-level confirmation that the advantage bug pattern is shared (Defect 1 above) covers the analytical concern.

---

## 4. Flows not tested this run

- **VtM clan edge cases**: Caitiff (no clan disciplines), Thin-Blood, Hecata / Tzimisce, Blood-Sorcery clans w/ rituals. Time spent on bug analysis crowded these out. Recommend adding to the next pass.
- **Werewolf**: full creation flow (Tribe, Auspice, Gifts, Renown, Rites), edit flow, multi-buy advantage undo (predicted to fail same as VtM).
- **Hunter**: full creation flow (Creed, Drives, Edges/Perks, Safe houses, Organizations, Loresheets), edit flow, multi-buy advantage undo.
- **Note-editing on XP log entries** (the brief mentions editing an entry's note and persisting it). Did not exercise.
- **Cross-cutting (Priority 3)**: Favorites, Folders, Cross-line search, PDF export, Public-view-vs-Edit-view permission checks. All untested.
- **Validation paths** (empty required fields, max values, min values) — untested. The Save Character button correctly stayed disabled until Touchstones+Convictions+Specialties+Disciplines were all filled, which is partial evidence of validation working, but I didn't try to defeat it.

---

## 5. Recommendations / follow-up

1. **Fix Defect 1 in all 3 game lines simultaneously.** Add a multi-record unit test to each of `vtm.test.js`, `werewolf.test.js`, `hunter.test.js`:
   ```js
   it("advantage multi-record + undo top: only the top entry's delta is reversed, not the session-start snapshot", () => { ... });
   ```
   The current 54-test suite shouldn't go green until that test passes — the safest fix is option 1 from Defect 1 (reverse by `delta`, not assign `priorValue`), since it's a single-line change per handler.
2. **Empirically reproduce Defect 1 on Werewolf and Hunter** before shipping — Hunter in particular has a subtly different state-construction pattern that may or may not exhibit the same intermediate-state bug.
3. **Defect 2 (NaN cost)** is one-line `v-if` or `?: 0`; bundle into the Defect 1 fix PR.
4. **Defect 3 (ads on route change)** is unrelated to this PR; file separately.
5. For the next QA pass, **prioritize the Werewolf and Hunter create-flows** plus clan/tribe edge cases — those were the largest gaps this run.

---

## 6. Test artifacts left behind

- VtM character id `103` (`QA Tester Vamp`, owned by `testxp@test.com`/user 42) — created during this test, can be deleted.
- DB user `qatester` (id 43, email `qa@test.local`) — created during initial auth troubleshooting, never actually used (the existing `testxp` account worked). Safe to delete.
- Screenshots in [2026-05-15-xp-log-screenshots/](2026-05-15-xp-log-screenshots/):
  - `vtm-xp-log-after-purchase.png`
  - `vtm-undo-confirm.png`
  - `vtm-adv-dot-bug-after-first-undo.png`
- The `qa-xp-log` worktree at `/home/chase/projects/SchreckNet/.worktrees/qa-xp-log/` and the `qa-xp-log` branch — both are throwaway, made fresh off `origin/main` for this run. (As of 2026-05-15 post-promotion to `docs/qa/`, this worktree can be removed: `git worktree remove .worktrees/qa-xp-log && git branch -D qa-xp-log`.)
- Dev servers (API on `:5000`, frontend on `:8080`) are still running in this worktree at the time of writing; kill with `pkill -f "node server.js"` and `pkill -f "quasar dev"` (or by background-task ID) when no longer needed.
